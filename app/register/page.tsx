"use client";
import Link from "next/link";
import { useState } from "react";
import { ArrowLeft, Mail, Lock, Phone, UserRound, BriefcaseBusiness } from "lucide-react";
import LanguageToggle, { Language } from "../components/LanguageToggle";
import "../globals.css";
export default function RegisterPage(){const [language,setLanguage]=useState<Language>("bn");const [role,setRole]=useState<"customer"|"provider">("customer");const [submitted,setSubmitted]=useState(false);const bn=language==="bn";const submit=(e:React.FormEvent<HTMLFormElement>)=>{e.preventDefault();const f=new FormData(e.currentTarget);const account={name:String(f.get("name")||""),phone:String(f.get("phone")||""),email:String(f.get("email")||"").trim(),password:String(f.get("password")||""),role};localStorage.setItem("laksam-account",JSON.stringify(account));localStorage.setItem("laksam-session",JSON.stringify({name:account.name,email:account.email,role}));setSubmitted(true)};return <main className="auth-page"><div className="auth-topbar"><Link className="auth-brand" href="/">Laksam <span>Local Service</span></Link><LanguageToggle onChange={setLanguage}/></div><div className="auth-card"><p className="eyebrow">{bn?"শুরু করুন":"GET STARTED"}</p><h1>{bn?"আপনার অ্যাকাউন্ট তৈরি করুন":"Create your account"}</h1><p>{bn?"কয়েকটি ধাপেই স্থানীয় সার্ভিস বুক করুন।":"Book trusted local services in just a few steps."}</p><div className="role-switch"><button type="button" className={role==="customer"?"active":""} onClick={()=>setRole("customer")}><UserRound size={17}/>{bn?"কাস্টমার":"Customer"}</button><button type="button" className={role==="provider"?"active":""} onClick={()=>setRole("provider")}><BriefcaseBusiness size={17}/>{bn?"প্রোভাইডার":"Provider"}</button></div>{role==="provider"?<div className="auth-success">{bn?"প্রোভাইডার হিসেবে প্রোফাইল তৈরি করুন।":"Create your provider profile."}<Link className="secondary-btn" href="/provider"><BriefcaseBusiness size={17}/> {bn?"Provider Registration":"Provider Registration"}</Link></div>:submitted?<div className="auth-success"><strong>{bn?"অ্যাকাউন্ট তৈরি হয়েছে!":"Account created!"}</strong><span>{bn?"এখন আপনার ড্যাশবোর্ডে যেতে পারবেন।":"Your account is ready."}</span><Link className="primary-btn" href="/dashboard">{bn?"ড্যাশবোর্ডে যান":"Go to dashboard"}</Link></div>:<form onSubmit={submit}><label>{bn?"পুরো নাম":"Full name"}<div className="input-icon"><UserRound size={17}/><input name="name" required placeholder={bn?"আপনার নাম":"Your name"}/></div></label><label>{bn?"ফোন নম্বর":"Phone number"}<div className="input-icon"><Phone size={17}/><input name="phone" required placeholder="01XXXXXXXXX"/></div></label><label>{bn?"ইমেইল":"Email address"}<div className="input-icon"><Mail size={17}/><input name="email" required type="email" placeholder="you@example.com"/></div></label><label>{bn?"পাসওয়ার্ড":"Password"}<div className="input-icon"><Lock size={17}/><input name="password" required minLength={4} type="password" placeholder={bn?"একটি পাসওয়ার্ড তৈরি করুন":"Create a password"}/></div></label><button className="primary-btn" type="submit">{bn?"অ্যাকাউন্ট তৈরি করুন":"Create account"}</button></form>}<p className="auth-switch">{bn?"আগেই অ্যাকাউন্ট আছে?":"Already have an account?"} <Link href="/login">{bn?"লগইন":"Login"}</Link></p><Link className="back-link auth-back" href="/"><ArrowLeft size={15}/> {bn?"হোমে ফিরুন":"Back to home"}</Link></div></main>}
